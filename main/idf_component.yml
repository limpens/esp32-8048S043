dependencies:
  lvgl/lvgl:
    version: "==8.3.11"
    rules:
      - if: "target == esp32s3"

  espressif/esp_lcd_touch_gt911:
    version: "==1.1.0"
    rules:
      - if: "target == esp32s3"

  idf:
    version: ">=5.1.0"
